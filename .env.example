# ── PostgreSQL (Camada Gold) ──────────────────────────────────
POSTGRES_USER=pipeline
POSTGRES_PASSWORD=pipeline123
POSTGRES_DB=ecommerce_gold
POSTGRES_HOST=localhost

# ── MongoDB (Fonte de reviews) ────────────────────────────────
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=ecommerce_raw

# ── LGPD — Salt para anonimização de PII ─────────────────────
# IMPORTANTE: Use um valor forte e aleatório em produção.
# Nunca commite o valor real. Gere com: python -c "import secrets; print(secrets.token_hex(32))"
PII_SALT=change_me_in_production_use_strong_random_value

# ── Airflow ───────────────────────────────────────────────────
AIRFLOW_UID=50000
